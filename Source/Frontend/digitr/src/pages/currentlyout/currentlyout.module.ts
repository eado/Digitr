import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CurrentlyoutPage } from './currentlyout';

@NgModule({
  declarations: [
  ],
  imports: [
    IonicPageModule.forChild(CurrentlyoutPage),
  ],
})
export class CurrentlyoutPageModule {}
